{
  "name": "attractors",
  "version": "1.0.0",
  "description": "Strange attractors in WebGL",
  "dependencies": {
    "babel-runtime": "^4.6.3",
    "gh-pagesify": "^0.2.0",
    "gl-mat4": "^1.1.2",
    "react": "^0.12.2"
  },
  "devDependencies": {
    "babel-core": "^4.6.3",
    "babel-loader": "^4.0.0",
    "webpack": "^1.5.3"
  },
  "scripts": {
    "start": "webpack --watch --progress --colors",
    "build": "rm -rf public && mkdir public && webpack && cp ./{index.html,index.css,bundle.js} public/",
    "deploy": "npm run build && gh-pagesify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shuhei Kagawa <shuhei.kagawa@gmail.com>",
  "license": "BSD",
  "gh-pages": {
    "repository": "shuhei/attractors"
  }
}
